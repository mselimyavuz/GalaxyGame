#:import menu menu

MainWidget:

<MainWidget>:
    canvas.before:
        Rectangle:
            size: self.size
            source: "images/bg1.jpg"
    menu_widget: menu_widget
    MenuWidget:
        id: menu_widget
    Label:
        text: root.score_txt
        font_name: "fonts/Eurostile.ttf"
        font_size: dp(20)
        size_hint: .18, .18
        pos_hint: {"x": 0, "top": 1}